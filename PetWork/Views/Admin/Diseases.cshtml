@model List<PetWork.Models.Disease>
@{
    ViewData["Title"] = "Hastalık Yönetimi";
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Hastalık Yönetimi</h1>
        <a asp-action="Index" class="btn btn-outline-secondary">Admin Paneline Dön</a>
    </div>
    <partial name="_Messages" />
    <div class="card shadow-sm"><div class="table-responsive">
        <table class="table table-hover align-middle mb-0">
            <thead><tr><th>Ad</th><th>Hayvan</th><th>Kategori</th><th>Görüntülenme</th><th class="text-end">İşlem</th></tr></thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td><td>@(item.AnimalType ?? item.PetType ?? "Genel")</td><td>@(item.Category ?? "-")</td><td>@item.ViewCount</td>
                    <td class="text-end"><a asp-action="EditContent" asp-route-type="disease" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary me-1"><i class="fas fa-edit me-1"></i>İncele / Düzenle</a><form asp-action="Delete" method="post" class="d-inline" onsubmit="return confirm('Bu hastalık kaydını kalıcı olarak silmek istiyor musunuz?');">
                        <input type="hidden" name="id" value="@item.Id" /><input type="hidden" name="type" value="disease" />
                        <button class="btn btn-sm btn-danger" type="submit"><i class="fas fa-trash me-1"></i>Sil</button>
                    </form></td>
                </tr>
            }
            </tbody>
        </table>
    </div></div>
</div>
